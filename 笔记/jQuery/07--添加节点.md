# 添加节点

`$("<span></span>");`

# 创建节点

1. **append()**--->添加到子元素的最后面
2. **appendTo()**--->同样也是添加到子元素的最后面，但是更推荐
3. prepend()--->添加到子元素的最前面
4. prependTo()--->同样添加到子元素的最前面，更推荐
5. after()--->添加到标签元素的后面，组成兄弟元素
6. before()--->添加到标签元素的前面

# 清空节点与删除节点

1. 清空内容：`empty();`

2. 移除节点：`remove();`

3. 克隆：

   - `clone(false/true);`

     --->false：和 不传参数都是深度复制，但不会复制事件

     --->true：也是深度复制，但会复制事件

​		   

​	